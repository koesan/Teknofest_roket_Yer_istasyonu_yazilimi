# Contributing to Ground Station Software

## Getting Started
1. Install dependencies: `pip install -r requirements.txt`
2. Run the application: `python yeristasyonu.py`

## Pull Requests
- Ensure the UI logic is separated from backend logic where possible.
- Test with sample serial data before submitting.
